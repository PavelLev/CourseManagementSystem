@using CourseManagementSystem.Models
@using Microsoft.AspNet.Identity
@model CourseManagementSystem.Models.CourseThread

<ul class="breadcrumb" style="background-color: white; padding-bottom:0px; margin-bottom:7px;">
    <li>
        @Html.ActionLink("Home", "Index", "Home")
    </li>
    <li>
        @Html.ActionLink(Model.Course.Name, Model.UserId == User.Identity.GetUserId() ? "Edit" : "Details", "Course", new { id = Model.CourseId }, null)
    </li>
    <li>
        @Html.ActionLink("Forum", "Forum", "Course", new { courseId = Model.CourseId }, null)
    </li>
    <li class="active">Topic</li>
</ul>

<hr />

<h2>Forum</h2>
<h3>@Model.Name</h3>

<div class="row">
    <div class="col-lg-10 col-md-10 col-sm-10 col-xs-9" style="padding:0px;">
    </div>
    <div class="col-lg-2 col-md-2 col-sm-2 col-xs-3" style="padding:0px;">
        <div class="text-right">
            <a href="#reply" class="btn btn-info" style="margin:0px;">Reply</a>
        </div>
    </div>
</div>
<div class="panel panel-primary" style="margin-top:20px;">
    <div class="panel-heading">
        @Model.Name
    </div>
    <div class="panel-body" style="padding: 0px; background-color: #446e9b">
        <div id="CourseMessages">
            @foreach (CourseMessage message in Model.Messages)
            {
                Html.RenderPartial("~/Views/CourseMessage/Bar.cshtml", message);
            }
        </div>
        @{
            Html.RenderPartial("~/Views/CourseMessage/Create.cshtml", new CourseMessage() {ThreadId = Model.CourseThreadId});
        }
    </div>

</div>
<p><a name="reply"></a></p>
<div class="row">
    <div class="col-lg-10 col-md-10 col-sm-10 col-xs-9" style="padding:0px;">
    </div>
    <div class="col-lg-2 col-md-2 col-sm-2 col-xs-3" style="padding:0px;">
        <div class="text-right">
            <a href="#top" class="btn btn-info btn-lg" style="border-radius:100%;margin:0px;">Up</a>
        </div>
    </div>
</div>

<hr />
<footer></footer>
